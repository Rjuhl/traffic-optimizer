#pragma once
#include "appmode.h"
#include <string>

class App {
    private:
    public:
        App();
        // App(AppMode mode);
        // App(const std::string& mapPath);
        // App(AppMode mode, const std::string& mapPath);
        // ~App();

        int run();
};